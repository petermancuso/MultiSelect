<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                xmlns:tools="http://schemas.android.com/tools">

    <CheckBox
        android:id="@+id/list_item_record_empty_check_box"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_alignParentRight="true"
        android:enabled="false"
        android:focusable="false"
        android:padding="4dp"/>
    <TextView
        android:id="@+id/list_item_record_title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/list_item_record_empty_check_box"
        android:textStyle="bold"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        tools:text="Crime title"/>
    <TextView
        android:id="@+id/list_item_record_date_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/list_item_record_title_text_view"
        android:layout_toLeftOf="@id/list_item_record_empty_check_box"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:paddingTop="4dp"
        tools:text="Crime Date"/>


</RelativeLayout>